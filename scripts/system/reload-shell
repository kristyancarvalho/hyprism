#!/usr/bin/env bash
set -Eeuo pipefail
config_name="${QS_CONFIG_NAME:-default}"
if qs -c "$config_name" ipc call shell reload >/dev/null 2>&1; then
  exit 0
fi
exec "${HYPRISM_ROOT:-$HOME/.local/share/hyprism}/scripts/system/start-shell"
